---
import cv from "@/cv.json";

const { education } = cv;
---

<section class="mt-12 print:mt-20">
  <h2 class="text-2xl font-bold">Educación</h2>

  <ul class="mt-3">
    {
      education.map(({ area, institution, startDate, endDate }) => (
        <li class="mt-5">
          <div class="flex items-center justify-between mb-1">
            <p class="font-bold text-[#000]">{institution}</p>
            <p>
              {startDate} - {endDate}
            </p>
          </div>
          <p>{area}</p>
        </li>
      ))
    }
  </ul>
</section>
